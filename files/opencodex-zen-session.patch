--- node_modules/@bitkyc08/opencodex/src/providers/opencode-go-transport.ts
+++ node_modules/@bitkyc08/opencodex/src/providers/opencode-go-transport.ts
@@ -26,1 +26,1 @@
- * Add Go affinity only to the canonical fixed-key destination.
+ * Add session affinity to the canonical OpenCode Go and Zen destinations.
@@ -38,1 +38,2 @@
-  if (registryEntryForProviderDestination(provider)?.id !== "opencode-go") return provider;
+  const destinationId = registryEntryForProviderDestination(provider)?.id;
+  if (destinationId !== "opencode-go" && destinationId !== "opencode-zen") return provider;
--- node_modules/@bitkyc08/opencodex/src/types/wire.ts
+++ node_modules/@bitkyc08/opencodex/src/types/wire.ts
@@ -48,3 +48,4 @@
 const ANTHROPIC_WIRE_MODELS: Record<string, ReadonlySet<string>> = {
   "opencode-go": new Set(["minimax-m2.5", "minimax-m2.7", "minimax-m3"]),
+  "opencode-zen": new Set(["union-alpha"]),
 };
